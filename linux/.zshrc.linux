# Extras for Linux machines (Thor, Ivaldi — NixOS)

# Full system upgrade — pull the latest everything-nix (CI bumps flake.lock
# daily) and rebuild NixOS. $(hostname) matches the flake output name.
alias uu='(cd ~/everything-nix && git pull && sudo nixos-rebuild switch --flake .#$(hostname))'
