Touch git only when I ask: no branching, committing, tagging, pushing, or pull requests on your own initiative, and don't offer them as a next step. When I ask, apply these conventions: branches `type/slug` (types feat, fix, refactor, docs, test, chore, perf, ci; slug lowercase, dash-separated, whole words, max 32 characters); commits `type(scope): subject` (`scope` optional), imperative, lowercase, no trailing period, max 72 characters, one logical change, files staged by path; pull requests titled `type: descriptive title`, body says why before what.
