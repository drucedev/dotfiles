This is Andrei’s professional environment at Europace.

Pull requests on `europace` or `hypoport` remotes carry the Jira ticket: title `type: [TICKET-123] descriptive title`, body's first line `Relates to [TICKET-123]` — "Relates to" only, never auto-close keywords. Take the ticket ID from the conversation or the branch name; never invent one. If none is available, ask; if I say the work is ticketless, drop the prefix and the link line. Branches and commits follow the general conventions.
