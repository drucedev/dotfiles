This is Andrei’s professional environment at Europace.

In repositories whose remote matches `europace` or `hypoport`, pull requests carry the Jira ticket: the title is `type: [TICKET-123] descriptive title` and the body’s first line is `Relates to [TICKET-123]`. Link tickets only with "Relates to" — never with auto-close keywords. Take the ticket ID from the conversation, or from the branch name when it happens to contain one; never invent one. When no ticket ID is available, ask; if I say the work is ticketless, omit the bracketed prefix and the link line. Branch names and commit messages follow the general conventions unchanged.
